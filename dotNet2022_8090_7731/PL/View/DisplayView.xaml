<Window x:Class="PL.View.DisplayView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.ViewModels"
        mc:Ignorable="d"
        Title="DisplayView" Height="450" Width="800">
    <Grid>
        <!--<ContentControl x:Name="DroneView" Content="{Binding }" ScrollViewer.VerticalScrollBarVisibility="Auto"/>-->

        <Button Content="ParcelList:" VerticalAlignment="Stretch"
                Height="46" Width="92" 
                Command="{Binding ParcelListViewCommand}"/>

        <Button Content="DroneList:" VerticalAlignment="Top"
                Height="46" Width="92" 
                Command="{Binding DroneListViewCommand}"/>

        <Button Content="stationList:" VerticalAlignment="Bottom"
                Height="46" Width="92" 
                Command="{Binding StationListViewCommand}"/>

        <Button Content="CustomerList:" VerticalAlignment="Top"
                 Height="46" Width="92" 
                Command="{Binding CustomerListViewCommand}" Margin="0,90,0,0" />
    </Grid>
</Window>
