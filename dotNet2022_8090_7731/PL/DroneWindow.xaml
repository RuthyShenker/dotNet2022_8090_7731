<Window x:Class="PL.DroneWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        mc:Ignorable="d"
        Title="DroneWindow" Height="450" Width="800">
    <StackPanel>
        <StackPanel x:Name="detailsOfDrone">

            <!--<TextBlock   Text = "!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!" TextWrapping = "Wrap" />
            <TextBox PreviewTextInput="TextBox_OnPreviewTextInput" />
            <TextBox Name = "NumberTextBox" PreviewTextInput = "NumberValidationTextBox" />-->

            <TextBlock Text = "Id:" />
            <TextBox x:Name = "IdTextBox" PreviewTextInput="TextBox_OnPreviewTextInput" Text = "{Binding Path=Id, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text = "Battery:" />
            <TextBox x:Name = "BatteryTextBox" Text = "0" IsEnabled="False"/>

            <TextBlock Text = "MaxWeight:" />
            <ComboBox x:Name = "MaxWeightComboBox" ItemsSource="{Binding}" SelectionChanged="MaxWeightComboBox_SelectionChanged" />

            <TextBlock Text = "Model:"/>
            <TextBox x:Name = "ModelTextBox" PreviewTextInput="TextBox_OnPreviewTextInput" Text = "{Binding Path=Model, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text = "Status:" />
            <ComboBox x:Name = "StatusComboBox" ItemsSource="{Binding}"/>

            <TextBlock Text = "Deliery:"/>
            <TextBox x:Name = "DeliveryTextBox" PreviewTextInput="TextBox_OnPreviewTextInput" Text = "{Binding Path=DeliveredParcelId, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text = "Latitude:" />
            <TextBox x:Name = "LatitudeTextBox" PreviewTextInput="TextBox_OnPreviewTextInput" Text = "{Binding Path=CurrLocation.Latitude, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text = "Longitude:" />
            <TextBox x:Name = "LongitudeTextBox" PreviewTextInput="TextBox_OnPreviewTextInput" Text = "{Binding Path=CurrLocation.Longitude, UpdateSourceTrigger=PropertyChanged}" TextWrapping = "Wrap" />
        </StackPanel>
        <!--Longitude
Latitude-->


        <StackPanel x:Name="GridOfAddDrone"  >
            <Button  x:Name="OkAdd" Content = "OK" Click = "Button_Click_Of_Adding_New_Drone" />
        </StackPanel>

        <StackPanel x:Name="GridOfUpdateDrone">
            <Button x:Name="UpdateModel" Content = "Update Model:"   />
            <Button x:Name="SendOrReleaseDroneFromCharging" Content = "Send Drone To Charge:" Click = "Button_Click_Of_Adding_New_Drone" />
            <Button x:Name="SendOrPickOrArrivalDrone" Content = "Send Drone To Delivery:"  Click = "Button_Click_Of_Adding_New_Drone"  />
        </StackPanel>
        <StackPanel x:Name="message" >
        </StackPanel>
        <TextBlock   Name="messageId"  />
    </StackPanel >
</Window >

